import { Depth } from "./styled";

const Thickness = () => (
    <>
      <Depth style={{transform: 'translateZ(2px)'}} />
      <Depth style={{transform: 'translateZ(2.5px)'}} />
      <Depth style={{transform: 'translateZ(3px)'}} />
      <Depth style={{transform: 'translateZ(3.5px)'}} />
      <Depth style={{transform: 'translateZ(4px)'}} />
    </>
    
  );

export default Thickness;